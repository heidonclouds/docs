# ТЗ: Сценарий 4 — Учёт излишков при следующем планировании

**Версия:** 1.0
**Дата:** 2026-06-05
**Адресат:** разработчик 1С (конфигурация MES Kitchen)
**Связь:** продолжение Сценариев 1–3. Использует регистры `ТоварыНаСкладахMES` и
`ТоварыВТранзитеMES`, перечисление видов движения и документ списания (Сценарий 1).

---

## 0. Назначение

Повар выпустил ≥ план, но один или несколько **чужих цехов не подтвердили приёмку**. Непринятый ПФ
**зависает в транзите** (выделено > принято). При следующем MRP система не знает об этом остатке —
и закажет лишний выпуск. Нужно дать повару решить судьбу остатка: **перенести на завтра** (учесть в
MRP) или **списать**.

> Остаток считается только по **чужим цехам** — у магазинов и своего цеха операции «принять» нет
> (для них выделено = принято автоматически).

**Источник остатка** — положительный баланс регистра `ТоварыВТранзитеMES`:
```
ОстатокТранзита(заказ, цех-получатель) = Выделено − Принято > 0
```

---

## 1. Триггер блока «Остаток»

В карточке завершённого RO (статус `Готов`) блок «⚠ Остаток у вас» появляется, когда хотя бы по
одному чужому цеху-получателю остаток транзита > 0. По каждому такому цеху — строка:
«[Цех] не принял X — выделено A, принято B».

В списке задач: бейдж «⚠ ОСТАТОК» (вместо «ГОТОВ») + подпись «Остаток X».

Действия (на весь блок): **«Перенести на завтра»** и **«Списать»**.

---

## 2. Действие «Перенести на завтра» (carry-over)

Непринятый остаток возвращается в склад цеха-производителя и датируется **следующим рабочим днём** —
так его увидит MRP следующего дня и уменьшит план выпуска.

**Эффект (по каждому зависшему цеху-получателю, на остаток `D = Выделено − Принято`):**

| Регистр | Движение |
|---|---|
| `ТоварыВТранзитеMES` | **Расход** `D` (`ВозвратИзТранзита`) — транзит закрывается |
| `ТоварыНаСкладахMES` | **Приход** `D` в `ЦехОтправитель`, период = **следующий рабочий день** (`ВозвратИзТранзита`) |

> Возврат именно в склад цеха-производителя («пусть он решает, где потерялось»). Дата прихода =
> следующий рабочий день → MRP этого дня учтёт остаток как доступный и уменьшит выпуск.

**Влияние на MRP следующего дня:**
```
Спрос(продукция, завтра)         = 300
Доступно (возврат из транзита)   = −50
К выпуску завтра                 = 250
```

После переноса: блок заменяется подтверждением «✅ Перенесено на завтра: X», бейдж → «ГОТОВ».

### 2.1. Документ `SS_ПереносОстаткаMES` (новый)

| Реквизит шапки | Тип | Комментарий |
|---|---|---|
| `Дата` / `Номер` | стандартные | Автонумерация |
| `Автор` | `CatalogRef.Пользователи` | Из сессии |
| `ЗаказОснование` | `DocumentRef.SS_ЗаказНаВыпускMES` | RO |
| `ЦехОтправитель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | `Заказ.Цех` |
| `Продукция` | `CatalogRef.Номенклатура` | `Заказ.Продукция` |
| `ЕдиницаИзмерения` | `CatalogRef.ЕдиницыИзмерения` | Базовая ЕИ |
| `ДатаПереноса` | `Дата` | Следующий рабочий день |

Табличная часть `Остатки`:

| Поле | Тип | Комментарий |
|---|---|---|
| `ЦехПолучатель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | Кто не принял |
| `Количество` | `Число (15,5)` | Остаток транзита `D` |

### 2.2. Псевдокод `ОбработкаПроведения`

```bsl
Процедура ОбработкаПроведения(Отказ, Режим)

    Движения.ТоварыВТранзитеMES.Записывать = Истина;
    Движения.ТоварыНаСкладахMES.Записывать = Истина;

    Для Каждого Стр Из Остатки Цикл
        Если Стр.Количество <= 0 Тогда Продолжить; КонецЕсли;

        // Закрываем транзит
        Тр = Движения.ТоварыВТранзитеMES.Добавить();
        Тр.ВидДвижения    = ВидДвиженияНакопления.Расход;
        Тр.Период         = Дата;
        Тр.ЗаказОснование = ЗаказОснование;
        Тр.ЦехОтправитель = ЦехОтправитель;
        Тр.ЦехПолучатель  = Стр.ЦехПолучатель;
        Тр.Номенклатура   = Продукция;
        Тр.Количество     = Стр.Количество;
        Тр.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ВозвратИзТранзита;

        // Возврат в склад отправителя, датируется днём переноса (для MRP)
        Скл = Движения.ТоварыНаСкладахMES.Добавить();
        Скл.ВидДвижения    = ВидДвиженияНакопления.Приход;
        Скл.Период         = НачалоДня(ДатаПереноса);
        Скл.Цех            = ЦехОтправитель;
        Скл.Номенклатура   = Продукция;
        Скл.Количество     = Стр.Количество;
        Скл.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ВозвратИзТранзита;
    КонецЦикла;

КонецПроцедуры
```

### 2.3. Эндпоинт `POST /carry-over`

**Тело запроса:**
```json
{ "ro_id": "<uuid RO>" }
```
Бэкенд сам определяет цеха с остатком транзита > 0, вычисляет `D = выделено − принято` по каждому и
`ДатаПереноса = следующий рабочий день`.

**Ответ `200`:**
```json
{
  "id": "<uuid документа переноса>",
  "ro_id": "<uuid>",
  "target_date": "2026-05-19",
  "items": [ { "consumer_unit_id": "<uuid>", "qty": 50 } ],
  "total_qty": 50
}
```

**Коды ошибок:** `400 VALIDATION_ERROR`, `404 ORDER_NOT_FOUND`,
`409 NO_TRANSIT_REMAINDER` (нет зависшего остатка), `500 INTERNAL_ERROR`.

### 2.4. BSL `ПереносОстаткаPost`

```bsl
Функция ПереносОстаткаPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Или СтруктураЗапроса.Свойство("ro_id") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан ro_id.", "VALIDATION_ERROR"));
    КонецЕсли;

    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.ro_id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный ro_id.", "VALIDATION_ERROR"));
    КонецПопытки;
    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    // Остатки транзита по чужим цехам: ТЗ с колонками ЦехПолучатель, Количество
    ТЗОстатки = SS_MESKitchen.ОстаткиТранзитаПоЗаказу(СсылкаЗаказ);
    Если ТЗОстатки.Количество() = 0 Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code", "Нет непринятого остатка.", "NO_TRANSIT_REMAINDER"));
    КонецЕсли;

    ДатаЗавтра = SS_MESKitchen.СледующийРабочийДень(НачалоДня(ТекущаяДата()));

    НачатьТранзакцию();
    Попытка
        Док = Документы.SS_ПереносОстаткаMES.СоздатьДокумент();
        Док.Дата             = ТекущаяДата();
        Док.Автор            = ПараметрыСеанса.ТекущийПользователь;
        Док.ЗаказОснование   = СсылкаЗаказ;
        Док.ЦехОтправитель   = СсылкаЗаказ.Цех;
        Док.Продукция        = СсылкаЗаказ.Продукция;
        Док.ЕдиницаИзмерения = СсылкаЗаказ.Продукция.БазоваяЕдиницаИзмерения;
        Док.ДатаПереноса     = ДатаЗавтра;

        Итого = 0;
        Элементы = Новый Массив;
        Для Каждого О Из ТЗОстатки Цикл
            Стр = Док.Остатки.Добавить();
            Стр.ЦехПолучатель = О.ЦехПолучатель;
            Стр.Количество    = О.Количество;
            Итого = Итого + О.Количество;
            Элементы.Добавить(Новый Структура("consumer_unit_id,qty",
                СокрЛП(О.ЦехПолучатель.УникальныйИдентификатор()), О.Количество));
        КонецЦикла;

        Док.Записать(РежимЗаписиДокумента.Проведение);
        ЗафиксироватьТранзакцию();

        Ответ = Новый Структура;
        Ответ.Вставить("id", СокрЛП(Док.Ссылка.УникальныйИдентификатор()));
        Ответ.Вставить("ro_id", СтруктураЗапроса.ro_id);
        Ответ.Вставить("target_date", Формат(ДатаЗавтра, "ДФ=yyyy-MM-dd"));
        Ответ.Вставить("items", Элементы);
        Ответ.Вставить("total_qty", Итого);
        Возврат ОтветJSON(200, Ответ);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code", "Ошибка транзакции переноса." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

---

## 3. Действие «Списать»

Списание непринятого остатка из транзита. Переиспользует документ `SS_СписаниеMES` (Сценарий 1),
но **источник — транзит** (а не склад). Тип: `Столовая` (staff) или `Переработка` (recycle).

**Эффект:**

| Тип | Движения |
|---|---|
| `Столовая` | `ТоварыВТранзитеMES`: Расход `qty` (`СписаниеПотери`) |
| `Переработка` | `ТоварыВТранзитеMES`: Расход `qty` (`СписаниеПереработкаРасход`) + `ТоварыНаСкладахMES`: Приход `target_qty` целевой номенклатуры в `ЦехОтправитель` (`ПриходПереработки`) |

> Отличие от Сценария 1: расход списываемого ПФ идёт **из транзита** (по паре заказ/отправитель/получатель),
> а не из склада цеха. В `SS_СписаниеMES` добавить признак источника (`ИзТранзита` = Истина) и поля
> `ЗаказОснование`, `ЦехПолучатель` для адресации строки транзита.

Параметры списания и сам эндпоинт — `POST /write-off` из Сценария 1 (с дополнительными полями для
адресации транзита: `consumer_unit_id`). Тип/целевая номенклатура/количество выхода — как в Сценарии 1.

---

## 4. Затронутые эндпоинты (сводка)

| Эндпоинт | Изменение | Сценарий |
|---|---|---|
| `POST /yield` | `consumers[]` + `shops_qty` (распределение) | 2 |
| `POST /receive` | приёмка ПФ из транзита | 3 |
| `POST /write-off` | списание; для остатка — источник транзит | 1, 4 |
| `POST /carry-over` | перенос непринятого остатка на след. день | 4 |
| `GET /executor/orders` | карточки `RECEIVE_TASK`; бейдж «ОСТАТОК» для RO с остатком транзита | 3, 4 |

---

## 5. Ограничения мини-МВП

| Что | Ограничение |
|---|---|
| Transfer Orders | Не используются — физическая передача вне MES |
| Приёмка | Ручной ввод без QR |
| CARRY_OVER | Только ручной перенос поваром; нет авто-расчёта в полночь |
| Уведомления цеху | «ПФ готов, приди забери» — вне MES |

---

## 6. Чек-лист

- [ ] Документ `SS_ПереносОстаткаMES`: шапка + ТЧ `Остатки` (п. 2.1).
- [ ] Проведение переноса: транзит− + склад отправителя+ с датой следующего рабочего дня (п. 2.2).
- [ ] Эндпоинт `POST /carry-over` — функция `ПереносОстаткаPost` (п. 2.3–2.4).
- [ ] Функции `ОстаткиТранзитаПоЗаказу`, `СледующийРабочийДень`.
- [ ] Списание остатка из транзита: расширить `SS_СписаниеMES` (`ИзТранзита`, `ЦехПолучатель`) и `/write-off` (`consumer_unit_id`) (п. 3).
- [ ] `GET /executor/orders`: бейдж «ОСТАТОК» для RO с остатком транзита по чужим цехам (п. 4).
- [ ] Проверка влияния на MRP: возврат датируется следующим рабочим днём (п. 2).
