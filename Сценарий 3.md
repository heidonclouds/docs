# ТЗ: Сценарий 3 — Приёмка входящего ПФ как отдельная задача

**Версия:** 1.0
**Дата:** 2026-06-05
**Адресат:** разработчик 1С (конфигурация MES Kitchen)
**Связь:** продолжение Сценариев 1–2. Использует регистр транзита `ТоварыВТранзитеMES` и
перечисление видов движения из Сценария 2.

---

## 0. Назначение

Чужой цех приготовил ПФ и физически привёз его в цех-получатель. Повар-потребитель должен
**подтвердить фактически полученное количество**. До подтверждения ПФ числится в **транзите**
(передан Сценарием 2, но не принят). Приёмка переводит ПФ из транзита в склад цеха-получателя.

> Transfer Orders / QR не используются (мини-МВП) — только ручной ввод фактически полученного.

**Эффект приёмки (на количество `R`):**

| Регистр | Движение |
|---|---|
| `ТоварыВТранзитеMES` | Расход `R` (`ПриёмкаИзТранзита`) — уходит из транзита |
| `ТоварыНаСкладахMES` | Приход `R` в склад цеха-получателя (`ПриёмкаИзТранзита`) |

Непринятая разница (`выделено − принято`) **остаётся в транзите** и разрешается в Сценарии 4
(перенос/списание). Приёмка её не трогает.

---

## 1. Триггер задачи приёмки

Карточка «📦 Принять ПФ» появляется в списке задач цеха-получателя, когда по этому цеху есть
**положительный остаток в транзите**:

```
Выделено(заказ, цех-получатель) − Принято(заказ, цех-получатель) > 0
```
что эквивалентно остатку регистра `ТоварыВТранзитеMES` по (Заказ, ЦехПолучатель) > 0.

Покрывает оба случая: первичная приёмка (принято = 0) и доприёмка (принято < выделено).
Карточка исчезает, когда остаток транзита = 0.

**Сортировка/фильтр** (для фронта): карточки приёмки идут после `Приготовление`/`ГотовЧастично`,
но выше `Обеспечен`/`Новый`. Фильтр «📦 ПФ N», где N = число карточек приёмки.

---

## 2. Данные для экрана приёмки

| Поле | Источник |
|---|---|
| Название ПФ | `Заказ.Продукция` |
| От кого | `Заказ.Цех` (цех-отправитель) + номер RO |
| «Выделено» | Σ `ПередачаВТранзит` по (заказ, цех-получатель) |
| «Принято ранее» | Σ `ПриёмкаИзТранзита` по (заказ, цех-получатель) |
| Поле ввода | «Фактически получено», база ЕИ |

Предупреждение, если принимают **меньше выделенного** (UI): «Принимаете меньше — разница остаётся
у производителя» (разрешено). Принять **больше** — также разрешено (предупреждения нет).

---

## 3. Документ `SS_ПриёмкаПФMES` (новый)

Фиксирует факт приёмки ПФ цехом-получателем. Создаётся эндпоинтом `POST /receive`.

### 3.1. Реквизиты шапки

| Реквизит | Тип | Комментарий |
|---|---|---|
| `Дата` / `Номер` | стандартные | Автонумерация |
| `Автор` | `CatalogRef.Пользователи` | Кто принял (из сессии) |
| `ЗаказОснование` | `DocumentRef.SS_ЗаказНаВыпускMES` | RO (контекст) |
| `ЦехОтправитель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | `Заказ.Цех` |
| `ЦехПолучатель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | Кто принимает |
| `Продукция` | `CatalogRef.Номенклатура` | `Заказ.Продукция` |
| `ЕдиницаИзмерения` | `CatalogRef.ЕдиницыИзмерения` | Базовая ЕИ |
| `Количество` | `Число (15,5)` | Фактически принято (`received_qty`) |
| `ДатаПланирования` | `Дата` | `Заказ.ДатаПланирования` |

### 3.2. Движения (`ОбработкаПроведения`)

| Регистр | Движение |
|---|---|
| `ТоварыВТранзитеMES` | **Расход** `Количество` по (ЗаказОснование, ЦехОтправитель, ЦехПолучатель, Продукция), `ПриёмкаИзТранзита` |
| `ТоварыНаСкладахMES` | **Приход** `Количество` в `ЦехПолучатель`, `ПриёмкаИзТранзита` |

> Если принято больше, чем в транзите (разрешено) — остаток транзита уйдёт в минус по этой паре.
> Если политика `РазрешитьОтрицательныеОстаткиПФ` выключена и приёмка превышает выделенное — вернуть
> `409 RECEIVE_EXCEEDS_TRANSIT` (на этапе MVP допустимо разрешить).

### 3.3. Псевдокод `ОбработкаПроведения`

```bsl
Процедура ОбработкаПроведения(Отказ, Режим)

    Движения.ТоварыВТранзитеMES.Записывать = Истина;
    Движения.ТоварыНаСкладахMES.Записывать = Истина;

    // Уход из транзита
    Тр = Движения.ТоварыВТранзитеMES.Добавить();
    Тр.ВидДвижения    = ВидДвиженияНакопления.Расход;
    Тр.Период         = Дата;
    Тр.ЗаказОснование = ЗаказОснование;
    Тр.ЦехОтправитель = ЦехОтправитель;
    Тр.ЦехПолучатель  = ЦехПолучатель;
    Тр.Номенклатура   = Продукция;
    Тр.Количество     = Количество;
    Тр.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ПриёмкаИзТранзита;

    // Приход в склад получателя
    Скл = Движения.ТоварыНаСкладахMES.Добавить();
    Скл.ВидДвижения    = ВидДвиженияНакопления.Приход;
    Скл.Период         = Дата;
    Скл.Цех            = ЦехПолучатель;
    Скл.Номенклатура   = Продукция;
    Скл.Количество     = Количество;
    Скл.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ПриёмкаИзТранзита;

КонецПроцедуры
```

---

## 4. Эндпоинт `POST /receive`

Создаёт и проводит документ `SS_ПриёмкаПФMES`.

**Тело запроса:**
```json
{
  "ro_id": "<uuid RO>",
  "consumer_unit_id": "<uuid цеха-получателя>",
  "received_qty": 2.2
}
```

**Ответ `200`:**
```json
{
  "id": "<uuid документа приёмки>",
  "ro_id": "<uuid>",
  "consumer_unit_id": "<uuid>",
  "received_qty": 2.2,
  "allocated_qty": 2.4,
  "received_total": 2.2,
  "created_at": "<дата>"
}
```

**Коды ошибок:**

| HTTP | code | Условие |
|---|---|---|
| 400 | `VALIDATION_ERROR` | Невалидный JSON / нет поля / неверный uuid |
| 404 | `ORDER_NOT_FOUND` | RO не найден |
| 422 | `INVALID_QUANTITY` | `received_qty ≤ 0` |
| 409 | `RECEIVE_EXCEEDS_TRANSIT` | Приёмка > выделенного (при включённом контроле) |
| 500 | `INTERNAL_ERROR` | Ошибка транзакции проведения |

### 4.1. BSL `ПриёмкаPost`

```bsl
Функция ПриёмкаPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Ошибка чтения Json." + Символы.ПС + ТекстОшибки, "VALIDATION_ERROR"));
    КонецЕсли;
    Для Каждого Поле Из СтрРазделить("ro_id,consumer_unit_id,received_qty", ",") Цикл
        Если СтруктураЗапроса.Свойство(Поле) = Ложь Тогда
            Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан " + Поле + ".", "VALIDATION_ERROR"));
        КонецЕсли;
    КонецЦикла;
    Если СтруктураЗапроса.received_qty <= 0 Тогда
        Возврат ОтветJSON(422, Новый Структура("error,code", "received_qty <= 0.", "INVALID_QUANTITY"));
    КонецЕсли;

    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.ro_id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный ro_id.", "VALIDATION_ERROR"));
    КонецПопытки;
    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    Попытка
        ЦехПолуч = Справочники.SS_ЦехаДляПроизводстваMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.consumer_unit_id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный consumer_unit_id.", "VALIDATION_ERROR"));
    КонецПопытки;

    НачатьТранзакцию();
    Попытка
        Док = Документы.SS_ПриёмкаПФMES.СоздатьДокумент();
        Док.Дата             = ТекущаяДата();
        Док.Автор            = ПараметрыСеанса.ТекущийПользователь;
        Док.ЗаказОснование   = СсылкаЗаказ;
        Док.ЦехОтправитель   = СсылкаЗаказ.Цех;
        Док.ЦехПолучатель    = ЦехПолуч;
        Док.Продукция        = СсылкаЗаказ.Продукция;
        Док.ЕдиницаИзмерения = СсылкаЗаказ.Продукция.БазоваяЕдиницаИзмерения;
        Док.Количество       = СтруктураЗапроса.received_qty;
        Док.ДатаПланирования = СсылкаЗаказ.ДатаПланирования;
        Док.Записать(РежимЗаписиДокумента.Проведение);

        ЗафиксироватьТранзакцию();

        Ответ = Новый Структура;
        Ответ.Вставить("id", СокрЛП(Док.Ссылка.УникальныйИдентификатор()));
        Ответ.Вставить("ro_id", СтруктураЗапроса.ro_id);
        Ответ.Вставить("consumer_unit_id", СтруктураЗапроса.consumer_unit_id);
        Ответ.Вставить("received_qty", Док.Количество);
        Ответ.Вставить("allocated_qty", SS_MESKitchen.ВыделеноПолучателю(СсылкаЗаказ, ЦехПолуч));
        Ответ.Вставить("received_total", SS_MESKitchen.ПринятоПолучателем(СсылкаЗаказ, ЦехПолуч));
        Ответ.Вставить("created_at", Док.Дата);
        Возврат ОтветJSON(200, Ответ);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code", "Ошибка транзакции приёмки." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

> `ВыделеноПолучателю` / `ПринятоПолучателем` — функции расчёта по движениям регистра транзита
> (`ПередачаВТранзит` и `ПриёмкаИзТранзита` по заказу и цеху-получателю).

---

## 5. `GET /executor/orders` — карточки приёмки

В список задач добавить элементы типа `RECEIVE_TASK` для каждой пары (RO, цех-получатель) с
остатком транзита > 0:

```json
{
  "type": "RECEIVE_TASK",
  "ro_id": "<uuid>",
  "consumer_unit_id": "<uuid цеха>",
  "product": "Бульон куриный",
  "from_workshop": "Мясной цех",
  "allocated_qty": 2.4,
  "received_qty": 0
}
```

Источник: остатки регистра `ТоварыВТранзитеMES` по цеху текущего пользователя.

---

## 6. Чек-лист

- [ ] Документ `SS_ПриёмкаПФMES`: шапка (п. 3.1), проведение в 2 регистра (п. 3.2–3.3).
- [ ] Эндпоинт `POST /receive` — функция `ПриёмкаPost` (п. 4–4.1).
- [ ] Функции `ВыделеноПолучателю` / `ПринятоПолучателем` из движений транзита.
- [ ] `GET /executor/orders`: карточки `RECEIVE_TASK` из остатков транзита по цеху (п. 5).
- [ ] (Опц.) Контроль `received > выделено` по настройке (`409 RECEIVE_EXCEEDS_TRANSIT`).
