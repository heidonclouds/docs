# ТЗ: Сценарий 2 — Частичный выпуск с распределением по потребителям

**Версия:** 2.0 (транзит-модель)
**Дата:** 2026-06-05
**Адресат:** разработчик 1С (конфигурация MES Kitchen)
**Связь:** продолжение «Сценарий 1». Вводит **регистр транзита** — общую основу для Сценариев 3 и 4.

---

## 0. Назначение

При **частичном выпуске** повар распределяет выпущенное между потребителями RO:
**магазины** (суммарно), **чужие цеха** (каждый отдельно), **свой цех**.

Распределение по **чужому цеху** — это не мгновенный приход к нему, а **передача в транзит**
(«выделено»). Физически продукт попадёт в склад получателя только после **приёмки** (Сценарий 3).
До приёмки он числится в транзите. Это исключает двойной учёт и даёт корректную картину остатков.

| Получатель | Что происходит при распределении |
|---|---|
| **Магазин** | Отгрузка: `−склад отправителя`, продукт покидает MES (у магазинов нет приёмки) |
| **Чужой цех** | Передача в транзит: `−склад отправителя`, `+транзит` («выделено») |
| **Свой цех** | Ничего: доля уже в складе своего цеха после выпуска (приёмка не нужна) |

Нераспределённый остаток остаётся в складе цеха-производителя.

---

## 1. Регистр `ТоварыВТранзитеMES` (новый)

Учёт продукции, переданной чужому цеху, но ещё не принятой им.

| Параметр | Значение |
|---|---|
| Вид регистра | **Остатки** |
| Назначение | Товары в пути между цехами (выделено, но не принято) |

### Измерения

| Измерение | Тип | Комментарий |
|---|---|---|
| `ЗаказОснование` | `DocumentRef.SS_ЗаказНаВыпускMES` | По какому RO передаём |
| `ЦехОтправитель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | Кто передал |
| `ЦехПолучатель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | Кому (только чужие цеха) |
| `Номенклатура` | `CatalogRef.Номенклатура` | Продукция |
| `Характеристика` | `CatalogRef.ХарактеристикиНоменклатуры` | Опц. |

### Ресурс

| Ресурс | Тип | Комментарий |
|---|---|---|
| `Количество` | `Число (15,5)` | Остаток в транзите (база ЕИ) |

### Реквизиты

| Реквизит | Тип | Назначение |
|---|---|---|
| `ВидДвижения` | `EnumRef.SS_ВидыДвиженийТоваровMES` | Аналитика |

> **Баланс транзита** по (Заказ, Отправитель, Получатель, Номенклатура) = `выделено − принято − возвращено − списано`.
> Положительный баланс = ещё не принятый остаток (источник для Сценариев 3 и 4).

---

## 2. Расширение перечисления `SS_ВидыДвиженийТоваровMES`

К видам из Сценария 1 добавить:

| Значение | Регистр / эффект | Сценарий |
|---|---|---|
| `ОтгрузкаВМагазин` | `ТоварыНаСкладахMES`: `−склад отправителя` (продукт уходит магазину) | 2 |
| `ПередачаВТранзит` | `ТоварыНаСкладахMES`: `−склад отправителя`; `ТоварыВТранзитеMES`: `+транзит` | 2 |
| `ПриёмкаИзТранзита` | `ТоварыВТранзитеMES`: `−транзит`; `ТоварыНаСкладахMES`: `+склад получателя` | 3 |
| `ВозвратИзТранзита` | `ТоварыВТранзитеMES`: `−транзит`; `ТоварыНаСкладахMES`: `+склад отправителя` | 4 |

---

## 3. Потребители заказа на выпуск

Плановая потребность по получателям — в табличной части заказа (`Основания` / `Потребители`):

| Поле | Тип | Назначение |
|---|---|---|
| `Получатель` | `СтруктурныеЕдиницы` + `SS_ЦехаДляПроизводстваMES` | Магазин или цех |
| `Основание` | `DocumentRef.SS_ЗаказНаВыпускMES` + `DocumentRef.ЗаказТоваров` | Заказ-потребитель |
| `КоличествоПлан` | `Число (15,5)` | Сколько нужно (из BOM) |
| `ЭтоСвойЦех` | `Булево` | Получатель = цех самого заказа |

> `КоличествоВыделено` / `КоличествоПринято` **не храним** — это динамика, считается из регистров:
> - **Выделено**(чужой цех) = Σ движений `ПередачаВТранзит` по (заказ, получатель);
> - **Принято**(чужой цех) = Σ движений `ПриёмкаИзТранзита` по (заказ, получатель).

---

## 4. Документ `SS_ПеремещениеMES` (распределение выпуска)

### 4.1. Реквизиты шапки

| Реквизит | Тип | Комментарий |
|---|---|---|
| `Дата` / `Номер` | стандартные | Автонумерация |
| `Автор` | `CatalogRef.Пользователи` | Из сессии |
| `ЗаказОснование` | `DocumentRef.SS_ЗаказНаВыпускMES` | RO |
| `ЦехОтправитель` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | `Заказ.Цех` |
| `Продукция` | `CatalogRef.Номенклатура` | `Заказ.Продукция` |
| `ЕдиницаИзмерения` | `CatalogRef.ЕдиницыИзмерения` | Базовая ЕИ |
| `ДатаПланирования` | `Дата` | `Заказ.ДатаПланирования` |

### 4.2. Табличная часть `Распределение`

| Поле | Тип | Комментарий |
|---|---|---|
| `Получатель` | `СтруктурныеЕдиницы` + `SS_ЦехаДляПроизводстваMES` | Магазин или цех |
| `ТипПолучателя` | `Перечисление` (Магазин/Цех) | Или определять по типу ссылки |
| `ЭтоСвойЦех` | `Булево` | Получатель = `ЦехОтправитель` |
| `Количество` | `Число (15,5)` | Сколько передаём (база ЕИ) |

### 4.3. Движения (`ОбработкаПроведения`)

Для каждой строки `Распределение` с `Количество > 0`:

| Получатель | Движения |
|---|---|
| **Магазин** | `ТоварыНаСкладахMES`: Расход из `ЦехОтправитель` (`ОтгрузкаВМагазин`) |
| **Чужой цех** | `ТоварыНаСкладахMES`: Расход из `ЦехОтправитель` (`ПередачаВТранзит`) **+** `ТоварыВТранзитеMES`: Приход (`ПередачаВТранзит`, ЦехПолучатель = строка.Получатель) |
| **Свой цех** | Движений нет (доля уже в складе своего цеха после выпуска) |

### 4.4. Псевдокод `ОбработкаПроведения`

```bsl
Процедура ОбработкаПроведения(Отказ, Режим)

    Движения.ТоварыНаСкладахMES.Записывать = Истина;
    Движения.ТоварыВТранзитеMES.Записывать = Истина;

    ТипЦех = Тип("СправочникСсылка.SS_ЦехаДляПроизводстваMES");

    Для Каждого Стр Из Распределение Цикл
        Если Стр.Количество <= 0 Тогда Продолжить; КонецЕсли;

        ЭтоЦех    = (ТипЗнч(Стр.Получатель) = ТипЦех);
        ЭтоСвой   = ЭтоЦех И (Стр.Получатель = ЦехОтправитель);

        Если ЭтоСвой Тогда
            Продолжить;  // доля своего цеха уже в его складе (после выпуска)
        КонецЕсли;

        // Расход из склада цеха-отправителя (всегда: и магазин, и чужой цех)
        РасходСклад = Движения.ТоварыНаСкладахMES.Добавить();
        РасходСклад.ВидДвижения    = ВидДвиженияНакопления.Расход;
        РасходСклад.Период         = Дата;
        РасходСклад.Цех            = ЦехОтправитель;
        РасходСклад.Номенклатура   = Продукция;
        РасходСклад.Количество     = Стр.Количество;
        РасходСклад.Получатель     = Стр.Получатель;
        РасходСклад.ВидДвиженияMES = ?(ЭтоЦех,
            Перечисления.SS_ВидыДвиженийТоваровMES.ПередачаВТранзит,
            Перечисления.SS_ВидыДвиженийТоваровMES.ОтгрузкаВМагазин);

        // Чужой цех -> в транзит
        Если ЭтоЦех Тогда
            ПриходТранзит = Движения.ТоварыВТранзитеMES.Добавить();
            ПриходТранзит.ВидДвижения    = ВидДвиженияНакопления.Приход;
            ПриходТранзит.Период         = Дата;
            ПриходТранзит.ЗаказОснование = ЗаказОснование;
            ПриходТранзит.ЦехОтправитель = ЦехОтправитель;
            ПриходТранзит.ЦехПолучатель  = Стр.Получатель;
            ПриходТранзит.Номенклатура   = Продукция;
            ПриходТранзит.Количество     = Стр.Количество;
            ПриходТранзит.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ПередачаВТранзит;
        КонецЕсли;
    КонецЦикла;

КонецПроцедуры
```

---

## 5. Расширение API

### 5.1. `POST /release-orders/yield`

```json
{
  "id": "<uuid RO>",
  "actual_qty": 21,
  "consumers": [ { "consumer_id": "<uuid цеха>", "qty": 4 } ],
  "shops_qty": 8
}
```

- `consumers[]` — распределение по цехам (включая свой). `consumer_id` = цех-получатель.
- `shops_qty` — суммарно на магазины; бэкенд разносит **пропорционально `КоличествоПлан`** магазинов RO.

**Валидация (в дополнение к Сценарию 1):**

| Правило | Ошибка |
|---|---|
| `SUM(consumers.qty) + shops_qty ≤ actual_qty` | `422 DISTRIBUTION_EXCEEDS_YIELD` |
| Любое `qty ≥ 0` | `422 INVALID_QUANTITY` |
| `consumer_id` есть среди потребителей RO | `422 UNKNOWN_CONSUMER` |

> `SUM < actual_qty` допустимо — разница остаётся в складе цеха-производителя.

**Действия (одна транзакция):**
1. Базовый `yield` (Сценарий 1): статус → `ГотовЧастично`, `КоличествоФакт`, пропорциональный расход
   сырья, запись в `Выпуск`, запись заказа **с проведением** (`РасходСырья` + `ВыпускПродукции`).
2. Разнести `shops_qty` по магазинам пропорционально плану.
3. Создать и провести `SS_ПеремещениеMES` (магазины = отгрузка, чужие цеха = транзит, свой цех = пропуск).

### 5.2. `POST /release-orders/complete`

При завершении остаток дораспределяется по плану автоматически (приоритет **Магазины → Чужие цеха → Свой цех**),
создаётся итоговое `SS_ПеремещениеMES`, заказ проводится со статусом `Готов` (логика статусов и
пропорционального расхода — Сценарий 1).

---

## 6. Код обработчиков (BSL)

### 6.1. `ВводВыходаPost` (расширение Сценария 1)

```bsl
Функция ВводВыходаPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Ошибка чтения Json." + Символы.ПС + ТекстОшибки, "VALIDATION_ERROR"));
    КонецЕсли;
    Если СтруктураЗапроса.Свойство("id") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан id.", "VALIDATION_ERROR"));
    КонецЕсли;
    Если СтруктураЗапроса.Свойство("actual_qty") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан actual_qty.", "VALIDATION_ERROR"));
    КонецЕсли;

    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный id.", "VALIDATION_ERROR"));
    КонецПопытки;
    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    СтП = Перечисления.SS_СтатусыЗаказовНаВыпускMES.Приготовление;
    СтГЧ = Перечисления.SS_СтатусыЗаказовНаВыпускMES.ГотовЧастично;
    Если СсылкаЗаказ.Статус <> СтП И СсылкаЗаказ.Статус <> СтГЧ Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code", "Статус не Приготовление/ГотовЧастично.", "INVALID_TRANSITION"));
    КонецЕсли;
    Если СтруктураЗапроса.actual_qty <= 0 Или СтруктураЗапроса.actual_qty > СсылкаЗаказ.Количество * 2 Тогда
        Возврат ОтветJSON(422, Новый Структура("error,code", "actual_qty <= 0 или > planned*2.", "INVALID_QUANTITY"));
    КонецЕсли;

    // Валидация распределения
    МассивПотр = ?(СтруктураЗапроса.Свойство("consumers"), СтруктураЗапроса.consumers, Новый Массив);
    СуммаЦеха = 0;
    Для Каждого П Из МассивПотр Цикл
        Если П.qty < 0 Тогда Возврат ОтветJSON(422, Новый Структура("error,code", "qty < 0.", "INVALID_QUANTITY")); КонецЕсли;
        СуммаЦеха = СуммаЦеха + П.qty;
    КонецЦикла;
    КолМаг = ?(СтруктураЗапроса.Свойство("shops_qty"), СтруктураЗапроса.shops_qty, 0);
    Если КолМаг < 0 Тогда Возврат ОтветJSON(422, Новый Структура("error,code", "shops_qty < 0.", "INVALID_QUANTITY")); КонецЕсли;
    Если СуммаЦеха + КолМаг > СтруктураЗапроса.actual_qty Тогда
        Возврат ОтветJSON(422, Новый Структура("error,code", "SUM(распределения) > actual_qty.", "DISTRIBUTION_EXCEEDS_YIELD"));
    КонецЕсли;

    НачатьТранзакцию();
    Попытка
        ОбЗаказ = СсылкаЗаказ.ПолучитьОбъект();
        ОбЗаказ.Статус = СтГЧ;
        КоличествоФакт = СтруктураЗапроса.actual_qty;
        КоличествоИзменение = КоличествоФакт - ОбЗаказ.КоличествоФакт;
        ОбЗаказ.КоличествоФакт = КоличествоФакт;
        ОбЗаказ.ДатаОбновления = ТекущаяДата();

        Коэф = ?(ОбЗаказ.Количество = 0, 0, ОбЗаказ.КоличествоФакт / ОбЗаказ.Количество);
        Для Каждого Стр Из ОбЗаказ.ЛокальныеИнгридиенты Цикл
            Стр.КоличествоФакт = Стр.Количество * Коэф;
        КонецЦикла;

        НВ = ОбЗаказ.Выпуск.Добавить();
        НВ.Пользователь = ПараметрыСеанса.ТекущийПользователь;
        НВ.ДатаСобытия  = ТекущаяДата();
        НВ.Количество   = КоличествоИзменение;
        НВ.Анулирована  = Ложь;

        ОбЗаказ.Записать(РежимЗаписиДокумента.Проведение);  // РасходСырья + ВыпускПродукции

        SS_MESKitchen.СоздатьПеремещениеРаспределения(ОбЗаказ, МассивПотр, КолМаг);

        ЗафиксироватьТранзакцию();

        Ответ = Новый Структура;
        Ответ.Вставить("id", СокрЛП(СсылкаЗаказ.УникальныйИдентификатор()));
        Ответ.Вставить("status", "ГотовЧастично");
        Ответ.Вставить("actual_qty", ОбЗаказ.КоличествоФакт);
        Ответ.Вставить("planned_qty", ОбЗаказ.Количество);
        Ответ.Вставить("yield_percent", ?(ОбЗаказ.Количество = 0, 0, Окр(ОбЗаказ.КоличествоФакт / ОбЗаказ.Количество * 100, 2)));
        Ответ.Вставить("updated_at", ОбЗаказ.ДатаОбновления);
        Возврат ОтветJSON(200, Ответ);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code", "Ошибка транзакции." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

### 6.2. `СоздатьПеремещениеРаспределения`

```bsl
Процедура СоздатьПеремещениеРаспределения(ОбЗаказ, МассивПотр, КолМаг) Экспорт

    Если МассивПотр.Количество() = 0 И КолМаг = 0 Тогда Возврат; КонецЕсли;

    Перем = Документы.SS_ПеремещениеMES.СоздатьДокумент();
    Перем.Дата             = ТекущаяДата();
    Перем.Автор            = ПараметрыСеанса.ТекущийПользователь;
    Перем.ЗаказОснование   = ОбЗаказ.Ссылка;
    Перем.ЦехОтправитель   = ОбЗаказ.Цех;
    Перем.Продукция        = ОбЗаказ.Продукция;
    Перем.ЕдиницаИзмерения = ОбЗаказ.Продукция.БазоваяЕдиницаИзмерения;
    Перем.ДатаПланирования = ОбЗаказ.ДатаПланирования;

    // Цеха из явного списка
    Для Каждого П Из МассивПотр Цикл
        Если П.qty <= 0 Тогда Продолжить; КонецЕсли;
        Цех = Справочники.SS_ЦехаДляПроизводстваMES.ПолучитьСсылку(Новый УникальныйИдентификатор(П.consumer_id));
        Стр = Перем.Распределение.Добавить();
        Стр.Получатель = Цех;
        Стр.ЭтоСвойЦех = (Цех = ОбЗаказ.Цех);
        Стр.Количество = П.qty;
    КонецЦикла;

    // Магазины пропорционально плану
    Если КолМаг > 0 Тогда
        ТЗМаг = ПолучитьПлановыхМагазинов(ОбЗаказ);   // Получатель, КоличествоПлан
        СуммаПлан = ТЗМаг.Итог("КоличествоПлан");
        Если СуммаПлан > 0 Тогда
            Разнесено = 0;
            Для Сч = 0 По ТЗМаг.Количество() - 1 Цикл
                М = ТЗМаг[Сч];
                Если Сч = ТЗМаг.Количество() - 1 Тогда
                    Доля = КолМаг - Разнесено;          // последнему — остаток
                Иначе
                    Доля = Окр(КолМаг * М.КоличествоПлан / СуммаПлан, 5);
                    Разнесено = Разнесено + Доля;
                КонецЕсли;
                Если Доля <= 0 Тогда Продолжить; КонецЕсли;
                Стр = Перем.Распределение.Добавить();
                Стр.Получатель = М.Получатель;
                Стр.ЭтоСвойЦех = Ложь;
                Стр.Количество = Доля;
            КонецЦикла;
        КонецЕсли;
    КонецЕсли;

    Перем.Записать(РежимЗаписиДокумента.Проведение);  // ОтгрузкаВМагазин / ПередачаВТранзит
КонецПроцедуры
```

### 6.3. `complete` — дораспределение по плану

К `ЗавершениеPost` (Сценарий 1) перед `ЗафиксироватьТранзакцию()` добавить:

```bsl
SS_MESKitchen.ДораспределитьПоПлану(ОбЗаказ);  // приоритет Магазины -> Чужие -> Свой
```

Процедура считает «выделено» по плану минус уже выделенное (из регистров), создаёт `SS_ПеремещениеMES`
на недостающее распределение в порядке приоритета (свой цех забирает остаток).

---

## 7. Чек-лист

- [ ] Регистр `ТоварыВТранзитеMES` (Остатки): измерения Заказ/ЦехОтправитель/ЦехПолучатель/Номенклатура/Характеристика, ресурс Количество (п. 1).
- [ ] Перечисление `SS_ВидыДвиженийТоваровMES`: `ОтгрузкаВМагазин`, `ПередачаВТранзит`, `ПриёмкаИзТранзита`, `ВозвратИзТранзита` (п. 2).
- [ ] Заказ: ТЧ потребителей с `КоличествоПлан`, `ЭтоСвойЦех` (п. 3).
- [ ] Функции «выделено/принято» из движений регистров (п. 3).
- [ ] Документ `SS_ПеремещениеMES`: шапка + ТЧ `Распределение`, проведение в 2 регистра (п. 4).
- [ ] Магазин = отгрузка; чужой цех = склад−/транзит+; свой цех = пропуск (п. 4.4).
- [ ] `/yield`: `consumers[]` + `shops_qty`, валидация, перемещение (п. 5.1, 6.1–6.2).
- [ ] `/complete`: авто-дораспределение по плану (п. 5.2, 6.3).
