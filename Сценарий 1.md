# ТЗ: Остаточная модель запасов MES (цеховые остатки)

**Версия:** 1.1
**Дата:** 2026-06-05
**Адресат:** разработчик 1С (конфигурация MES Kitchen)

---

## 0. Назначение

Ввести учёт **остатков товаров (ингредиентов и полуфабрикатов) по цехам** в реальном времени.
Остаток цеха меняется при:

1. **Выпуске продукции** по заказу на выпуск (`SS_ЗаказНаВыпускMES`) — частичном и итоговом:
   - приход выпущенной продукции/ПФ в цех;
   - расход израсходованного сырья из цеха (пропорционально выпуску).
2. **Списании** (`SS_СписаниеMES`) — оперативная фиксация брака/потерь в процессе работы:
   - тип «Столовая/потери» — только расход;
   - тип «Переработка» — расход исходного + приход целевого ПФ.

Объём ТЗ — три объекта:

1. Регистр накопления `ТоварыНаСкладахMES` (архитектура).
2. Обработка проведения документа `SS_ЗаказНаВыпускMES` (движения по выпуску).
3. Документ `SS_СписаниеMES` + его проводки + эндпоинт `POST /write-off`.

> **Единицы измерения.** В системе учёт номенклатуры (сырьё, ПФ, продукция) ведётся **всегда в базовых
> единицах измерения**. Поэтому конвертация ЕИ не требуется: все количества в документах и движениях —
> уже в базовой ЕИ. В регистре отдельного измерения по единице нет.

---

## 1. Регистр накопления `ТоварыНаСкладахMES`

### 1.1. Тип и общие свойства

| Параметр | Значение |
|---|---|
| Вид регистра | **Остатки** (AccumulationRegister, РежимИспользования = Остатки) |
| Назначение | Остатки номенклатуры по цехам |
| Единицы хранения | Базовая ЕИ номенклатуры (без отдельного измерения) |

### 1.2. Измерения

| Измерение | Тип | Обяз. | Комментарий |
|---|---|---|---|
| `Цех` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | да | Место хранения остатка |
| `Номенклатура` | `CatalogRef.Номенклатура` | да | Что лежит |
| `Характеристика` | `CatalogRef.ХарактеристикиНоменклатуры` | нет | Если ведётся учёт по характеристикам |

> Единица измерения **не** является измерением: количество всегда в базовой ЕИ номенклатуры →
> один остаток на пару (Цех, Номенклатура[, Характеристика]).

### 1.3. Ресурсы

| Ресурс | Тип | Комментарий |
|---|---|---|
| `Количество` | `Число (15,5)` | Остаток в базовой ЕИ номенклатуры |

### 1.4. Реквизиты

| Реквизит | Тип | Назначение |
|---|---|---|
| `ВидДвижения` | `EnumRef.SS_ВидыДвиженийТоваровMES` | Аналитика хозоперации (для отчётов и разбора) |

### 1.5. Перечисление `SS_ВидыДвиженийТоваровMES`

| Значение | Смысл | Источник |
|---|---|---|
| `ВыпускПродукции` | Приход результата выпуска | Заказ на выпуск, приход |
| `РасходСырья` | Расход ингредиентов на производство | Заказ на выпуск, расход |
| `СписаниеПотери` | Списание в столовую/потери | Списание, тип «Столовая» |
| `СписаниеПереработкаРасход` | Расход исходного ПФ при переработке | Списание, тип «Переработка», расход |
| `ПриходПереработки` | Приход целевого ПФ при переработке | Списание, тип «Переработка», приход |

### 1.6. Регистраторы

- `Document.SS_ЗаказНаВыпускMES`
- `Document.SS_СписаниеMES`

### 1.7. Политика отрицательных остатков

Контроль отрицательного остатка ПФ управляется настройкой (константа/настройка `РазрешитьОтрицательныеОстаткиПФ`):

- разрешено уходить в минус → проводки выполняются без блокировки;
- запрещено → при расходе, уводящем остаток цеха в минус, проведение завершается ошибкой
  (`INSUFFICIENT_STOCK`), транзакция откатывается.

> На этапе MVP допустимо разрешить минус. Контроль — отдельной итерацией.

---

## 2. Обработка проведения `SS_ЗаказНаВыпускMES`

Документ уже подключён к регистру (`RegisterRecords → AccumulationRegister.ТоварыНаСкладахMES`,
`Posting = Allow`, `RegisterRecordsDeletion = AutoDeleteOnUnpost`, `RegisterRecordsWritingOnPost = WriteSelected`).

### 2.1. Когда формируются движения

Движения регистра формируются в `ОбработкаПроведения` при **каждой записи проведённого документа**.
Поскольку 1С при перепроведении **полностью пересоздаёт** движения документа, набор движений всегда
отражает **текущее кумулятивное состояние** заказа (а не дельту конкретного выпуска).

Это означает: и при частичном выпуске (`yield` → статус `ГотовЧастично`), и при итоговом
(`complete` → статус `Готов`) логика построения движений **одна и та же** — она читает текущие
`КоличествоФакт` шапки и строк.

> **Табличная часть `Выпуск`** (события частичных выпусков: Пользователь, ДатаСобытия, Количество-дельта,
> Анулирована) — это **журнал аудита** действий повара. Для движений регистра она **не используется**:
> регистр строится из текущего состояния документа, а не из суммы событий.

### 2.2. Коэффициент выпуска

```
Коэффициент = ?(Количество = 0, 0, КоличествоФакт / Количество)
```
где `Количество` — план шапки, `КоличествоФакт` — фактически выпущено на данный момент.

### 2.3. Пропорциональный расход сырья

Сырьё списывается **пропорционально** фактическому выпуску (а не полным рецептурным объёмом).

В обработчиках yield/complete перед записью документа для каждой строки `ЛокальныеИнгридиенты`
рассчитывается фактический расход:

```
СтрокаИнгр.КоличествоФакт = СтрокаИнгр.Количество * Коэффициент
```

Так таблица `ЛокальныеИнгридиенты.КоличествоФакт` и движения регистра остаются согласованными.

> **Изменение текущего кода:** в действующей функции `complete` всем строкам проставляется
> `КоличествоФакт = Количество` (полная рецептура). Заменить на пропорциональный расчёт выше,
> чтобы и частичный, и итоговый выпуск списывали сырьё пропорционально факту.

### 2.4. Состав движений

При проведении (для текущего `КоличествоФакт`):

| Движение | Вид | Номенклатура | Количество | Цех | `ВидДвижения` |
|---|---|---|---|---|---|
| Приход продукции | **Приход** | `Продукция` (шапка) | `КоличествоФакт` шапки | `Цех` | `ВыпускПродукции` |
| Расход сырья (по строке) | **Расход** | `ЛокальныеИнгридиенты.Номенклатура` | `СтрокаИнгр.КоличествоФакт` | `Цех` | `РасходСырья` |

Все движения — в **один цех** (`Цех` шапки). Несколько строк сырья → несколько движений расхода.

### 2.5. Псевдокод `ОбработкаПроведения`

```bsl
Процедура ОбработкаПроведения(Отказ, Режим)

    Движения.ТоварыНаСкладахMES.Записывать = Истина;

    Коэффициент = ?(Количество = 0, 0, КоличествоФакт / Количество);

    // 1. Приход выпущенной продукции в цех
    Если КоличествоФакт > 0 Тогда
        Движение = Движения.ТоварыНаСкладахMES.Добавить();
        Движение.ВидДвижения    = ВидДвиженияНакопления.Приход;
        Движение.Период         = Дата;
        Движение.Цех            = Цех;
        Движение.Номенклатура   = Продукция;
        Движение.Характеристика = Характеристика;
        Движение.Количество     = КоличествоФакт;
        Движение.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ВыпускПродукции;
    КонецЕсли;

    // 2. Пропорциональный расход сырья из цеха
    Для Каждого Стр Из ЛокальныеИнгридиенты Цикл
        Если Стр.КоличествоФакт <= 0 Тогда Продолжить; КонецЕсли;

        Движение = Движения.ТоварыНаСкладахMES.Добавить();
        Движение.ВидДвижения    = ВидДвиженияНакопления.Расход;
        Движение.Период         = Дата;
        Движение.Цех            = Цех;
        Движение.Номенклатура   = Стр.Номенклатура;
        Движение.Характеристика = Стр.Характеристика;
        Движение.Количество     = Стр.КоличествоФакт;
        Движение.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.РасходСырья;
    КонецЦикла;

    // (опц.) контроль отрицательного остатка по настройке РазрешитьОтрицательныеОстаткиПФ

КонецПроцедуры
```

### 2.6. Сценарий частичных выпусков (пример)

План: 200 шт круассанов. Рецептура на 200 шт: мука 10 кг.

| Действие | КоличествоФакт шапки | Коэф. | Приход продукции | Расход муки |
|---|---|---|---|---|
| yield 45 | 45 | 0.225 | +45 шт | −2.25 кг |
| yield → 120 (всего) | 120 | 0.600 | +120 шт | −6.0 кг |
| complete (итог 120) | 120 | 0.600 | +120 шт | −6.0 кг |

> На каждом шаге документ перепроводится, движения пересоздаются под новый `КоличествоФакт`.
> Итоговое состояние регистра = последний набор движений (приход 120 шт, расход 6 кг).

---

## 3. Документ `SS_СписаниеMES` + проводки

### 3.1. Назначение

Оперативное списание из остатков цеха в процессе работы по заказу (брак, потери, переработка).
Создаётся эндпоинтом `POST /write-off`. При проведении пишет движения в `ТоварыНаСкладахMES`.

### 3.2. Реквизиты шапки

| Реквизит | Тип | Обяз. | Комментарий |
|---|---|---|---|
| `Дата` / `Номер` | стандартные | — | Автонумерация (как у заказа на выпуск) |
| `Автор` | `CatalogRef.Пользователи` | да | Кто списал (из сессии) |
| `Цех` | `CatalogRef.SS_ЦехаДляПроизводстваMES` | да | Цех списания (из заказа-основания) |
| `ЗаказОснование` | `DocumentRef.SS_ЗаказНаВыпускMES` | да | Контекст (ro_id) |
| `ВидСписания` | `EnumRef.SS_ВидыСписанияMES` | да | `Столовая` \| `Переработка` |
| `Номенклатура` | `CatalogRef.Номенклатура` | да | Что списываем |
| `Характеристика` | `CatalogRef.ХарактеристикиНоменклатуры` | нет | — |
| `ЕдиницаИзмерения` | `CatalogRef.ЕдиницыИзмерения` | да | Базовая ЕИ номенклатуры (заполняется автоматически) |
| `Количество` | `Число (15,5)` | да | Сколько списываем (`qty`), в базовой ЕИ |
| `ЦелеваяНоменклатура` | `CatalogRef.Номенклатура` | для `Переработка` | Во что перерабатываем |
| `ЦелеваяХарактеристика` | `CatalogRef.ХарактеристикиНоменклатуры` | нет | — |
| `ЦелеваяЕдиницаИзмерения` | `CatalogRef.ЕдиницыИзмерения` | для `Переработка` | Базовая ЕИ целевой номенклатуры |
| `КоличествоВыхода` | `Число (15,5)` | для `Переработка` | Сколько целевого выходит (`target_qty`) |
| `ДатаПланирования` | `Дата` | да | Плановый день (как у заказа) |

### 3.3. Перечисление `SS_ВидыСписанияMES`

| Значение | Код API (`type`) | Смысл |
|---|---|---|
| `Столовая` | `staff` | Питание сотрудников, брак, усушка — продукт исчезает из остатков цеха |
| `Переработка` | `recycle` | Брак → другая номенклатура (расход исходного + приход целевого) |

### 3.4. Движения регистра (`ОбработкаПроведения`)

**Тип `Столовая` (`staff`):** одно движение — расход.

| Движение | Вид | Номенклатура | Количество | Цех | `ВидДвижения` |
|---|---|---|---|---|---|
| Списание | **Расход** | `Номенклатура` | `Количество` | `Цех` | `СписаниеПотери` |

**Тип `Переработка` (`recycle`):** атомарно два движения.

| Движение | Вид | Номенклатура | Количество | Цех | `ВидДвижения` |
|---|---|---|---|---|---|
| Расход исходного | **Расход** | `Номенклатура` | `Количество` | `Цех` | `СписаниеПереработкаРасход` |
| Приход целевого | **Приход** | `ЦелеваяНоменклатура` | `КоличествоВыхода` | `Цех` | `ПриходПереработки` |

> Оба движения — в одном документе → одна транзакция проведения (атомарность гарантирована).

### 3.5. Псевдокод `ОбработкаПроведения`

```bsl
Процедура ОбработкаПроведения(Отказ, Режим)

    Движения.ТоварыНаСкладахMES.Записывать = Истина;

    // Расход списываемой номенклатуры (всегда)
    Расход = Движения.ТоварыНаСкладахMES.Добавить();
    Расход.ВидДвижения    = ВидДвиженияНакопления.Расход;
    Расход.Период         = Дата;
    Расход.Цех            = Цех;
    Расход.Номенклатура   = Номенклатура;
    Расход.Характеристика = Характеристика;
    Расход.Количество     = Количество;
    Расход.ВидДвиженияMES = ?(ВидСписания = Перечисления.SS_ВидыСписанияMES.Переработка,
                              Перечисления.SS_ВидыДвиженийТоваровMES.СписаниеПереработкаРасход,
                              Перечисления.SS_ВидыДвиженийТоваровMES.СписаниеПотери);

    // Приход целевого ПФ (только переработка)
    Если ВидСписания = Перечисления.SS_ВидыСписанияMES.Переработка Тогда
        Приход = Движения.ТоварыНаСкладахMES.Добавить();
        Приход.ВидДвижения    = ВидДвиженияНакопления.Приход;
        Приход.Период         = Дата;
        Приход.Цех            = Цех;
        Приход.Номенклатура   = ЦелеваяНоменклатура;
        Приход.Характеристика = ЦелеваяХарактеристика;
        Приход.Количество     = КоличествоВыхода;
        Приход.ВидДвиженияMES = Перечисления.SS_ВидыДвиженийТоваровMES.ПриходПереработки;
    КонецЕсли;

КонецПроцедуры
```

### 3.6. Эндпоинт `POST /write-off`

Веб-сервис `hs/mes-kitchen`, ресурс `/write-off`. Создаёт и проводит документ `SS_СписаниеMES`.

**Тело запроса:**
```json
{
  "ro_id": "<uuid заказа на выпуск>",
  "nomenclature_id": "<uuid списываемой номенклатуры>",
  "qty": 10,
  "type": "staff",                       // "staff" | "recycle"
  "target_nomenclature_id": "<uuid>",    // только для recycle
  "target_qty": 0.5                      // только для recycle
}
```

**Ответ `200`:**
```json
{
  "id": "<uuid созданного документа списания>",
  "ro_id": "<uuid заказа>",
  "type": "staff",
  "nomenclature_id": "<uuid>",
  "qty": 10,
  "target_nomenclature_id": "<uuid|null>",
  "target_qty": 0.5,
  "created_at": "<дата>"
}
```

**Коды ошибок (как в существующих yield/complete):**

| HTTP | code | Условие |
|---|---|---|
| 400 | `VALIDATION_ERROR` | Невалидный JSON / нет обязательного поля / неизвестный `type` / неверный uuid |
| 404 | `ORDER_NOT_FOUND` | Заказ по `ro_id` не найден |
| 409 | `INVALID_TRANSITION` | Статус заказа не Приготовление/ГотовЧастично |
| 409 | `INSUFFICIENT_STOCK` | Недостаточно остатка (при включённом контроле) |
| 422 | `INVALID_QUANTITY` | `qty <= 0`, либо для recycle `target_qty <= 0` |
| 500 | `INTERNAL_ERROR` | Ошибка транзакции проведения |

---

## 4. Код обработчиков API (BSL)

Ниже — реализация обработчиков веб-сервиса в стиле действующих функций.
Блоки выпуска (`yield`) и завершения (`complete`) приведены **с правками** под остаточную модель
(пропорциональный расход + запись с проведением). Списание (`write-off`) — новый обработчик.

### 4.1. `/release-orders/yield` — `ВводВыходаPost` (с правками)

```bsl
Функция ВводВыходаPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code",
            "Ошибка чтения Json тела запроса." + Символы.ПС + ТекстОшибки, "VALIDATION_ERROR"));
    КонецЕсли;

    Если СтруктураЗапроса.Свойство("id") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан обязательный параметр id.", "VALIDATION_ERROR"));
    КонецЕсли;

    Если СтруктураЗапроса.Свойство("actual_qty") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан обязательный параметр actual_qty.", "VALIDATION_ERROR"));
    КонецЕсли;

    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный id заказа.", "VALIDATION_ERROR"));
    КонецПопытки;

    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ на выпуск не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    СтатусПриготовление = Перечисления.SS_СтатусыЗаказовНаВыпускMES.Приготовление;
    СтатусГотовЧастично = Перечисления.SS_СтатусыЗаказовНаВыпускMES.ГотовЧастично;
    Если СсылкаЗаказ.Статус <> СтатусПриготовление И СсылкаЗаказ.Статус <> СтатусГотовЧастично Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code",
            "Статус заказа отличается от требуемого (Приготовление, ГотовЧастично).", "INVALID_TRANSITION"));
    КонецЕсли;

    Если СтруктураЗапроса.actual_qty <= 0 Или СтруктураЗапроса.actual_qty > СсылкаЗаказ.Количество * 2 Тогда
        Возврат ОтветJSON(422, Новый Структура("error,code",
            "Неверное значение actual_qty (<= 0 или > planned_qty * 2).", "INVALID_QUANTITY"));
    КонецЕсли;

    НачатьТранзакцию();
    Попытка
        ОбЗаказ = СсылкаЗаказ.ПолучитьОбъект();
        ОбЗаказ.Статус = СтатусГотовЧастично;
        КоличествоФакт = СтруктураЗапроса.actual_qty;
        КоличествоИзменение = КоличествоФакт - ОбЗаказ.КоличествоФакт;
        ОбЗаказ.КоличествоФакт = КоличествоФакт;
        ОбЗаказ.ДатаОбновления = ТекущаяДата();

        // --- ПРАВКА: пропорциональный расход сырья ---
        Коэффициент = ?(ОбЗаказ.Количество = 0, 0, ОбЗаказ.КоличествоФакт / ОбЗаказ.Количество);
        Для Каждого Стр Из ОбЗаказ.ЛокальныеИнгридиенты Цикл
            Стр.КоличествоФакт = Стр.Количество * Коэффициент;
        КонецЦикла;
        // ---------------------------------------------

        НовВыпуск = ОбЗаказ.Выпуск.Добавить();
        НовВыпуск.Пользователь = ПараметрыСеанса.ТекущийПользователь;
        НовВыпуск.ДатаСобытия  = ТекущаяДата();
        НовВыпуск.Количество   = КоличествоИзменение;
        НовВыпуск.Анулирована  = Ложь;

        // --- ПРАВКА: запись С ПРОВЕДЕНИЕМ -> движения регистра ТоварыНаСкладахMES ---
        ОбЗаказ.Записать(РежимЗаписиДокумента.Проведение);

        ЗафиксироватьТранзакцию();

        СтруктураОтвет = Новый Структура;
        СтруктураОтвет.Вставить("id", СокрЛП(СсылкаЗаказ.УникальныйИдентификатор()));
        СтруктураОтвет.Вставить("status", "ГотовЧастично");
        СтруктураОтвет.Вставить("actual_qty", ОбЗаказ.КоличествоФакт);
        СтруктураОтвет.Вставить("planned_qty", ОбЗаказ.Количество);
        СтруктураОтвет.Вставить("yield_percent",
            ?(ОбЗаказ.Количество = 0, 0, Окр(ОбЗаказ.КоличествоФакт / ОбЗаказ.Количество * 100, 2)));
        СтруктураОтвет.Вставить("updated_at", ОбЗаказ.ДатаОбновления);
        Возврат ОтветJSON(200, СтруктураОтвет);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code",
            "Ошибка транзакции записи заказа." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

### 4.2. `/release-orders/complete` — `ЗавершениеPost` (с правками)

```bsl
Функция ЗавершениеPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code",
            "Ошибка чтения Json тела запроса." + Символы.ПС + ТекстОшибки, "VALIDATION_ERROR"));
    КонецЕсли;

    Если СтруктураЗапроса.Свойство("id") = Ложь Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code", "Не указан обязательный параметр id.", "VALIDATION_ERROR"));
    КонецЕсли;

    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный id заказа.", "VALIDATION_ERROR"));
    КонецПопытки;

    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ на выпуск не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    Если СсылкаЗаказ.Статус = Перечисления.SS_СтатусыЗаказовНаВыпускMES.Готов Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code", "Заказ уже завершён.", "ORDER_ALREADY_COMPLETED"));
    КонецЕсли;

    СтатусПриготовление = Перечисления.SS_СтатусыЗаказовНаВыпускMES.Приготовление;
    СтатусГотовЧастично = Перечисления.SS_СтатусыЗаказовНаВыпускMES.ГотовЧастично;
    Если СсылкаЗаказ.Статус <> СтатусПриготовление И СсылкаЗаказ.Статус <> СтатусГотовЧастично Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code",
            "Статус заказа отличается от требуемого (Приготовление, ГотовЧастично).", "INVALID_TRANSITION"));
    КонецЕсли;

    НачатьТранзакцию();
    Попытка
        ОбЗаказ = СсылкаЗаказ.ПолучитьОбъект();

        Если СтруктураЗапроса.Свойство("actual_qty") = Ложь Тогда
            КоличествоФакт = ?(ОбЗаказ.КоличествоФакт = 0, ОбЗаказ.Количество, ОбЗаказ.КоличествоФакт);
        Иначе
            КоличествоФакт = СтруктураЗапроса.actual_qty;
        КонецЕсли;

        КоличествоИзменение = КоличествоФакт - ОбЗаказ.КоличествоФакт;
        ОбЗаказ.Статус         = Перечисления.SS_СтатусыЗаказовНаВыпускMES.Готов;
        ОбЗаказ.ДатаОбновления  = ТекущаяДата();
        ОбЗаказ.ДатаЗавершения  = ТекущаяДата();
        ОбЗаказ.КоличествоФакт  = КоличествоФакт;

        // --- ПРАВКА: пропорциональный расход вместо полной рецептуры ---
        Коэффициент = ?(ОбЗаказ.Количество = 0, 0, ОбЗаказ.КоличествоФакт / ОбЗаказ.Количество);
        Для Каждого Стр Из ОбЗаказ.ЛокальныеИнгридиенты Цикл
            Стр.КоличествоФакт = Стр.Количество * Коэффициент;
        КонецЦикла;
        // ---------------------------------------------------------------

        НовВыпуск = ОбЗаказ.Выпуск.Добавить();
        НовВыпуск.Пользователь = ПараметрыСеанса.ТекущийПользователь;
        НовВыпуск.ДатаСобытия  = ТекущаяДата();
        НовВыпуск.Количество   = КоличествоИзменение;
        НовВыпуск.Анулирована  = Ложь;

        // --- ПРАВКА: запись С ПРОВЕДЕНИЕМ ---
        ОбЗаказ.Записать(РежимЗаписиДокумента.Проведение);

        Если ОбЗаказ.Внутренний Тогда
            SS_MESKitchen.УстановитьОбеспечениеПоОснованиям(ОбЗаказ);
        КонецЕсли;

        ЗафиксироватьТранзакцию();

        СтруктураОтвет = Новый Структура;
        СтруктураОтвет.Вставить("id", СокрЛП(СсылкаЗаказ.УникальныйИдентификатор()));
        СтруктураОтвет.Вставить("status", "Готов");
        СтруктураОтвет.Вставить("actual_qty", ОбЗаказ.КоличествоФакт);
        СтруктураОтвет.Вставить("completed_at", ОбЗаказ.ДатаЗавершения);
        Возврат ОтветJSON(200, СтруктураОтвет);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code",
            "Ошибка транзакции записи заказа." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

### 4.3. `/write-off` — `СписаниеPost` (новый)

```bsl
Функция СписаниеPost(Запрос)
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    ТекстОшибки = "";
    СтруктураЗапроса = SS_MESKitchen.ИзСтрокиJson(ТелоЗапроса, ТекстОшибки);

    Если СтруктураЗапроса = Неопределено Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code",
            "Ошибка чтения Json тела запроса." + Символы.ПС + ТекстОшибки, "VALIDATION_ERROR"));
    КонецЕсли;

    // Обязательные поля
    Для Каждого Поле Из СтрРазделить("ro_id,nomenclature_id,qty,type", ",") Цикл
        Если СтруктураЗапроса.Свойство(Поле) = Ложь Тогда
            Возврат ОтветJSON(400, Новый Структура("error,code",
                "Не указан обязательный параметр " + Поле + ".", "VALIDATION_ERROR"));
        КонецЕсли;
    КонецЦикла;

    Тип = НРег(СокрЛП(СтруктураЗапроса.type));
    Если Тип <> "staff" И Тип <> "recycle" Тогда
        Возврат ОтветJSON(400, Новый Структура("error,code",
            "Неизвестный type (ожидается staff | recycle).", "VALIDATION_ERROR"));
    КонецЕсли;

    Если СтруктураЗапроса.qty <= 0 Тогда
        Возврат ОтветJSON(422, Новый Структура("error,code", "qty должно быть > 0.", "INVALID_QUANTITY"));
    КонецЕсли;

    Если Тип = "recycle" Тогда
        Если СтруктураЗапроса.Свойство("target_nomenclature_id") = Ложь Тогда
            Возврат ОтветJSON(400, Новый Структура("error,code",
                "Для recycle обязателен target_nomenclature_id.", "VALIDATION_ERROR"));
        КонецЕсли;
        Если СтруктураЗапроса.Свойство("target_qty") = Ложь Или СтруктураЗапроса.target_qty <= 0 Тогда
            Возврат ОтветJSON(422, Новый Структура("error,code",
                "Для recycle target_qty должно быть > 0.", "INVALID_QUANTITY"));
        КонецЕсли;
    КонецЕсли;

    // Заказ-основание
    Попытка
        СсылкаЗаказ = Документы.SS_ЗаказНаВыпускMES.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.ro_id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный ro_id.", "VALIDATION_ERROR"));
    КонецПопытки;

    Если СсылкаЗаказ.ПолучитьОбъект() = Неопределено Тогда
        Возврат ОтветJSON(404, Новый Структура("error,code", "Заказ на выпуск не найден.", "ORDER_NOT_FOUND"));
    КонецЕсли;

    Если СсылкаЗаказ.Статус <> Перечисления.SS_СтатусыЗаказовНаВыпускMES.Приготовление
       И СсылкаЗаказ.Статус <> Перечисления.SS_СтатусыЗаказовНаВыпускMES.ГотовЧастично Тогда
        Возврат ОтветJSON(409, Новый Структура("error,code",
            "Списание доступно только в статусах Приготовление / ГотовЧастично.", "INVALID_TRANSITION"));
    КонецЕсли;

    // Списываемая номенклатура
    Попытка
        Номенклатура = Справочники.Номенклатура.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.nomenclature_id));
    Исключение
        Возврат ОтветJSON(400, Новый Структура("error,code", "Неверный nomenclature_id.", "VALIDATION_ERROR"));
    КонецПопытки;

    НачатьТранзакцию();
    Попытка
        Док = Документы.SS_СписаниеMES.СоздатьДокумент();
        Док.Дата             = ТекущаяДата();
        Док.Автор            = ПараметрыСеанса.ТекущийПользователь;
        Док.Цех              = СсылкаЗаказ.Цех;
        Док.ЗаказОснование   = СсылкаЗаказ;
        Док.ДатаПланирования = СсылкаЗаказ.ДатаПланирования;
        Док.Номенклатура     = Номенклатура;
        Док.ЕдиницаИзмерения = Номенклатура.БазоваяЕдиницаИзмерения;
        Док.Количество       = СтруктураЗапроса.qty;

        Если Тип = "recycle" Тогда
            Док.ВидСписания = Перечисления.SS_ВидыСписанияMES.Переработка;
            ЦельНом = Справочники.Номенклатура.ПолучитьСсылку(Новый УникальныйИдентификатор(СтруктураЗапроса.target_nomenclature_id));
            Док.ЦелеваяНоменклатура     = ЦельНом;
            Док.ЦелеваяЕдиницаИзмерения = ЦельНом.БазоваяЕдиницаИзмерения;
            Док.КоличествоВыхода        = СтруктураЗапроса.target_qty;
        Иначе
            Док.ВидСписания = Перечисления.SS_ВидыСписанияMES.Столовая;
        КонецЕсли;

        Док.Записать(РежимЗаписиДокумента.Проведение);  // движения регистра в ОбработкаПроведения

        ЗафиксироватьТранзакцию();

        СтруктураОтвет = Новый Структура;
        СтруктураОтвет.Вставить("id", СокрЛП(Док.Ссылка.УникальныйИдентификатор()));
        СтруктураОтвет.Вставить("ro_id", СокрЛП(СсылкаЗаказ.УникальныйИдентификатор()));
        СтруктураОтвет.Вставить("type", Тип);
        СтруктураОтвет.Вставить("nomenclature_id", СтруктураЗапроса.nomenclature_id);
        СтруктураОтвет.Вставить("qty", Док.Количество);
        СтруктураОтвет.Вставить("target_nomenclature_id",
            ?(Тип = "recycle", СтруктураЗапроса.target_nomenclature_id, Неопределено));
        СтруктураОтвет.Вставить("target_qty", ?(Тип = "recycle", Док.КоличествоВыхода, Неопределено));
        СтруктураОтвет.Вставить("created_at", Док.Дата);
        Возврат ОтветJSON(200, СтруктураОтвет);
    Исключение
        ОтменитьТранзакцию();
        Возврат ОтветJSON(500, Новый Структура("error,code",
            "Ошибка транзакции проведения списания." + Символы.ПС + ОписаниеОшибки(), "INTERNAL_ERROR"));
    КонецПопытки;
КонецФункции
```

---

## 5. Краткий чек-лист для разработчика 1С

- [ ] Перечисление `SS_ВидыДвиженийТоваровMES` (5 значений, п. 1.5).
- [ ] Перечисление `SS_ВидыСписанияMES` (Столовая, Переработка).
- [ ] Регистр `ТоварыНаСкладахMES`: измерения Цех/Номенклатура/Характеристика, ресурс Количество, реквизит ВидДвижения (п. 1).
- [ ] `ОбработкаПроведения` заказа на выпуск: приход продукции + пропорциональный расход сырья (п. 2.5).
- [ ] Правки `yield`: пропорциональный расход + запись с проведением (п. 4.1).
- [ ] Правки `complete`: пропорциональный расход вместо полной рецептуры + запись с проведением (п. 4.2).
- [ ] Документ `SS_СписаниеMES` (реквизиты п. 3.2), подключить к регистру.
- [ ] `ОбработкаПроведения` списания: staff = расход; recycle = расход + приход (п. 3.5).
- [ ] Эндпоинт `POST /write-off` — функция `СписаниеPost` (п. 4.3).
- [ ] (Опц.) Контроль отрицательных остатков по настройке `РазрешитьОтрицательныеОстаткиПФ`.
